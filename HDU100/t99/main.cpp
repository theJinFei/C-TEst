#include <iostream>
#include <vector>
#include <cmath>
#include <stdio.h>
#include <map>
#define PI 3.1415927
using namespace std;
int isLeapYear(int year)
{
    if(year % 400 == 0 || (year % 4 == 0 && year % 100 != 0)){
        return 1;
    }else {
        return 0;
    }
}
int Prime[1000002]; // 第i个素数
int prime[1000002]; //返回n以内的
void Biao()
{

}
int main()
{
    int n, m;
    Biao();
    while(scanf("%d %d",&n,&m) != EOF){


    }

    return 0;
}
